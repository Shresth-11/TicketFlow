services:
  - type: web
    name: ticketflow-backend
    env: java
    rootDir: ticketflow-backend
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/ticketflow-backend-0.0.1-SNAPSHOT.jar
    plan: free
    envVars:
      - key: PORT
        value: 8080
